# Decision log (20260423_124302)

Generated: 2026-04-23 12:43:02 MDT

## Owner decisions required

| Topic | Status | Notes |
| --- | --- | --- |
| OAuth / API credential rotation for keys ever present in `MJS-Financial-Dash` history | **pending owner** | History rewrite does not revoke issued tokens; complete in provider consoles. |
| `Divorce` private remote | **optional** | `clean` locally; add private `origin` + `github_repo_url` in `config/repos.json` when ready. |
| **2024 Taxes** / **Bankruptcy** repos (unborn) | **defer** | No commits yet; defer initialization until recovery-core drift batches progress; see Batch D. |

## Automation / structural

| Topic | Status | Notes |
| --- | --- | --- |
| Backup vs canonical MJS remote collision | **pass** | `python3 scripts/check_remote_collisions.py` reports no risky duplicate origins (archive uses dedicated backup remote). |

## Batch A classification (summary)

- Repos with working tree noise: **17**.
- Largest untracked count this scan: **3** (Resume Builder).

---

## Detailed untracked samples

Per-repo **sample** of untracked lines (`git status --porcelain`, first 25). Use for Batch A triage; not exhaustive.

## Resume Builder (`Resume Builder`)

- `?? data/assets/README.md`
- `?? data/files_manifest.json`
- `?? data/os_registry_snapshot.json`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## Archiavellian (`Producer`)

- `?? ARCHIAVELLIAN/data/assets/README.md`
- `?? ARCHIAVELLIAN/data/files_manifest.json`
- `?? ARCHIAVELLIAN/data/os_registry_snapshot.json`
- `?? data/assets/README.md`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## Aneumind and TC Structure (`Aneumind and TC Structure`)

- `(**3** untracked paths; full paths omitted in standup log for `legal-financial-restricted` — run `git status` locally in `Aneumind and TC Structure`.)`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## App Builder (`App Builder/App Builder`)

- `?? data/assets/README.md`
- `?? data/files_manifest.json`
- `?? data/os_registry_snapshot.json`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## Archiavellian-Archive (`Archiavellian-Archive`)

- `(**3** untracked paths; full paths omitted in standup log for `legal-financial-restricted` — run `git status` locally in `Archiavellian-Archive`.)`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## Combat Injury Post-Incident Medical Tracking & VA Claims Documentation System (`Combat Injury Post-Incident Medical Tracking & VA Claims Documentation System`)

- `(**3** untracked paths; full paths omitted in standup log for `regulated-sensitive` — run `git status` locally in `Combat Injury Post-Incident Medical Tracking & VA Claims Documentation System`.)`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## Divorce (`Divorce`)

- `(**3** untracked paths; full paths omitted in standup log for `legal-financial-restricted` — run `git status` locally in `Divorce`.)`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## MJS Financial Dash (`MJS Financial Dash`)

- `(**3** untracked paths; full paths omitted in standup log for `legal-financial-restricted` — run `git status` locally in `MJS Financial Dash`.)`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## MJS Financial Dash Backup (`MJS Financial Dash backup 20260310_153810`)

- `(**3** untracked paths; full paths omitted in standup log for `legal-financial-restricted` — run `git status` locally in `MJS Financial Dash backup 20260310_153810`.)`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## MJSDS Dashboard (`GitHub/mjsds_dashboard`)

- `?? data/assets/README.md`
- `?? data/files_manifest.json`
- `?? data/os_registry_snapshot.json`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## MJSDS Website (`mjsds-website`)

- `?? data/assets/README.md`
- `?? data/files_manifest.json`
- `?? data/os_registry_snapshot.json`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## Momentum-OS (`Momentum-OS`)

- `?? data/assets/README.md`
- `?? data/files_manifest.json`
- `?? data/os_registry_snapshot.json`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## provider-access-hub (`provider-access-hub`)

- `(**3** untracked paths; full paths omitted in standup log for `regulated-sensitive` — run `git status` locally in `provider-access-hub`.)`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## Teach - Home Learning Playbook (`App Builder/Teach/home-learning-playbook`)

- `(**3** untracked paths; full paths omitted in standup log for `family-sensitive` — run `git status` locally in `App Builder/Teach/home-learning-playbook`.)`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## Teach - Zahmeir Learning System (`App Builder/Teach/zahmeir-learning-system`)

- `(**3** untracked paths; full paths omitted in standup log for `family-sensitive` — run `git status` locally in `App Builder/Teach/zahmeir-learning-system`.)`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## TuneFab (`TuneFab`)

- `(**3** untracked paths; full paths omitted in standup log for `archive-sensitive` — run `git status` locally in `TuneFab`.)`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

## Wayne Strain (`Wayne Strain`)

- `?? data/assets/README.md`
- `?? data/files_manifest.json`
- `?? data/os_registry_snapshot.json`

**Classification prompt:** PUSH (sanitized portfolio artifact) / LOCAL-ONLY (secrets, raw legal, PII) / TEMPLATE-CANDIDATE / IGNORE-via-gitignore (generated or machine-local).

