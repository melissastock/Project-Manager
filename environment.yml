name: base
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - flake8
  - pytest
  # Add any pip-only packages under `pip` (replace [] with e.g. `- requests`).
  - pip: []
